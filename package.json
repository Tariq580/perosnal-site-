{
  "name": "arian-archive",
  "version": "1.0.0",
  "description": "A monochrome personal archive built from Markdown",
  "scripts": {
    "build": "node scripts/build.js",
    "dev": "nodemon --watch content --watch scripts --watch index.template.html --watch app.js --watch styles.css --ext md,js,html,css --exec 'npm run build'",
    "new": "node scripts/new-article.js"
  },
  "dependencies": {
    "gray-matter": "^4.0.3",
    "marked": "^11.1.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  },
  "keywords": [
    "archive",
    "markdown",
    "personal-site",
    "static-site"
  ],
  "author": "Arian",
  "license": "MIT"
}
